# Astrovoyage Character Sheet — clean Phase 1

xThis is a clean Next.js Pages Router project intended for a new repository.

Run locally with:

npm install
npm run dev

The app uses localforage for browser-local persistence. No server/database is required in this phase.

The character data model uses the five Astrovoyage characteristics and the exact skill names supplied in the rules.

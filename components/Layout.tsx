import Link from 'next/link'; import {useRouter} from 'next/router'; import type {ReactNode} from 'react';
export default function Layout({children}:{children:ReactNode}){const r=useRouter();const m=String(r.asPath||'').match(/^\/character\/([^/?#]+)/);return <main className="terminal"><header className="top"><Link href="/" className="brand">ASTROVOYAGE</Link><div className="status">● LOCAL STATE // ONLINE</div>{m&&<nav style={{display:'flex',gap:6,marginLeft:'auto'}}><Link href={`/character/${m[1]}`} style={{color:r.asPath===`/character/${m[1]}`?'var(--blue-bright)':'var(--muted)',textDecoration:'none',fontSize:9,letterSpacing:1}}>PAGE 01</Link><Link href={`/character/${m[1]}/inventory`} style={{color:r.asPath.includes('/inventory')?'var(--blue-bright)':'var(--muted)',textDecoration:'none',fontSize:9,letterSpacing:1}}>PAGE 02 // INVENTORY</Link></nav>}</header><div className="content">{children}</div></main>}